import 'package:flutter_test/flutter_test.dart';

// Sensor-dependent widget tests require a real device or platform channel
// mocks. Smoke tests will be added in Phase 4 once mock sensor infrastructure
// is in place. Unit tests for the sensor math live in test/unit/.
void main() {
  test('placeholder — tests expanded in Phase 4', () {
    expect(true, isTrue);
  });
}
